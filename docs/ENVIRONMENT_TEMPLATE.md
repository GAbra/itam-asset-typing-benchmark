# Experiment environment

- Experiment ID / UTC date:
- Source commit and dirty-worktree status:
- CPU model / physical cores / logical processors:
- Physical RAM:
- Host OS and version:
- Java vendor / full version / JVM:
- JVM flags / heap limits / garbage collector:
- Docker version / image ID or digest / architecture (if used):
- Container CPU limit / memory limit / host virtualization:
- Power mode / background load / other running workloads:
- Dataset count / seed / SHA-256 / generation sidecar:
- Ruleset version / enabled rule count / SHA-256:
- Warmup iterations / warmup records / measured passes / batch / max:
- Exact commands:
- Verification report and label coverage:
- Raw benchmark reports (all runs):
- Notes and limitations:

Record unknown values as unknown. Keep different configurations separate and avoid
publishing usernames, private paths, environment variables or production data.
